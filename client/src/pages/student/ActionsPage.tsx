export default function ActionsPage() {
  return (
    <div className="text-center py-12">
      <h2 className="text-2xl font-bold text-[hsl(var(--foreground))]">Student Actions</h2>
      <p className="mt-2 text-[hsl(var(--muted-foreground))]">Request gate passes, file complaints, and more.</p>
    </div>
  );
}
