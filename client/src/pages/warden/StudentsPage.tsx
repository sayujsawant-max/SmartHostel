export default function StudentsPage() {
  return (
    <div>
      <h2 className="text-2xl font-bold text-[hsl(var(--foreground))]">Students</h2>
      <p className="mt-2 text-[hsl(var(--muted-foreground))]">Student directory and management.</p>
    </div>
  );
}
