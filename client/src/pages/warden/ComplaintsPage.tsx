export default function ComplaintsPage() {
  return (
    <div>
      <h2 className="text-2xl font-bold text-[hsl(var(--foreground))]">Complaints</h2>
      <p className="mt-2 text-[hsl(var(--muted-foreground))]">Review and manage student complaints.</p>
    </div>
  );
}
