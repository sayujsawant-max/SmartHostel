export default function DashboardPage() {
  return (
    <div>
      <h2 className="text-2xl font-bold text-[hsl(var(--foreground))]">Warden Dashboard</h2>
      <p className="mt-2 text-[hsl(var(--muted-foreground))]">Overview of hostel operations, pending approvals, and alerts.</p>
    </div>
  );
}
