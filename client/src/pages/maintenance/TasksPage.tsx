export default function TasksPage() {
  return (
    <div className="text-center py-12">
      <h2 className="text-2xl font-bold text-[hsl(var(--foreground))]">Tasks</h2>
      <p className="mt-2 text-[hsl(var(--muted-foreground))]">Your assigned maintenance tasks.</p>
    </div>
  );
}
